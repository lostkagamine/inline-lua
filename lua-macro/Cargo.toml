[package]
name = "lua-macro"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
mlua = { version = "0.7.4", features = ["lua54"] }
quote = "1.0.15"
lua-macro-impl = { path = "../lua-macro-impl" }
